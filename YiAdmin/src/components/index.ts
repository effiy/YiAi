import YiInputNumber from "./SearchForm/components/YiInputNumber.vue";

// 全局注册组件
const registerComponents = app => {
  app.component("YiInputNumber", YiInputNumber);
};

export default registerComponents;
