# 需求文档：{{功能名称}}

## 1. 需求概述

**功能名称**：{{功能名称}}

**需求类型**：{{新功能/功能改进/功能移除}}

**优先级**：{{高/中/低}}

**背景**：
{{描述为什么需要这个功能，当前的问题或痛点是什么}}

## 2. 功能分析

### 当前功能分析（如适用）

#### 2.1 文件结构
```
{{当前相关文件的结构，如：
src/
├── api/routes/
│   └── example.py
├── services/
│   └── example_service.py
└── models/
    └── schemas.py
}}
```

#### 2.2 功能特点
{{当前功能的特点和实现方式}}

### 新功能分析

#### 2.3 功能描述
{{详细描述新功能要做什么}}

#### 2.4 功能流程
1. **流程1**：
   - 步骤1
   - 步骤2
   - 步骤3

2. **流程2**：
   - 步骤1
   - 步骤2
   - 步骤3

## 3. API 设计

### 3.1 新增的端点

| 方法 | 路径 | 描述 |
|------|------|------|
| {{GET/POST/PUT/DELETE}} | {{路径，如：/api/v1/example}} | {{描述}} |

### 3.2 请求示例

#### {{端点1}} 请求
```bash
curl -X {{METHOD}} http://localhost:8000{{path}} \
  -H "Content-Type: application/json" \
  -H "X-Token: your-token" \
  -d '{{请求体 JSON}}'
```

### 3.3 响应示例

#### 成功响应
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    {{响应数据结构}}
  }
}
```

#### 错误响应
```json
{
  "code": {{错误码}},
  "msg": "{{错误信息}}",
  "data": null
}
```

## 4. 实施范围

### 4.1 需要新增的文件

| 文件路径 | 功能描述 |
|----------|----------|
| `{{文件路径，如：src/api/routes/example.py}}` | {{文件功能描述}} |

### 4.2 需要修改的文件

| 文件路径 | 修改内容 |
|----------|----------|
| `{{文件路径}}` | {{修改内容描述}} |

### 4.3 需要删除的文件（如适用）

| 文件路径 | 原因 |
|----------|------|
| `{{文件路径}}` | {{删除原因}} |

## 5. 数据模型

### 5.1 新增的 Pydantic 模型

```python
{{模型定义代码}}
```

### 5.2 数据库集合变更

| 集合名称 | 变更类型 | 说明 |
|---------|---------|------|
| {{集合名}} | {{新增/修改}} | {{说明}} |

## 6. 实施步骤

### 步骤 1：准备工作
{{准备阶段需要做的事情，如：创建分支、安装依赖等}}

### 步骤 2：核心开发
{{核心开发阶段的任务，如：实现 API 端点、编写服务逻辑等}}

### 步骤 3：集成测试
{{集成测试阶段的任务，如：编写测试用例、执行测试等}}

### 步骤 4：文档更新
{{需要更新的文档，如：API 文档、配置文档等}}

## 7. 影响分析

### 7.1 功能影响
- {{对现有功能的影响}}
- {{新增的功能能力}}

### 7.2 性能影响
- {{对性能的预期影响}}
- {{性能优化措施}}

### 7.3 兼容性影响
- {{对 API 兼容性的影响}}
- {{对数据兼容性的影响}}
- {{对配置兼容性的影响}}

## 8. 风险评估

### 8.1 风险识别

| 风险类型 | 风险级别 | 影响程度 | 发生概率 | 缓解措施 |
|---------|---------|---------|---------|---------|
| {{风险类型，如：数据迁移风险}} | {{高/中/低}} | {{高/中/低}} | {{高/中/低}} | {{缓解措施}} |

### 8.2 风险缓解

#### {{风险类型1}}缓解
- {{缓解措施1}}
- {{缓解措施2}}

## 9. 验收标准

### 9.1 功能验证
- [ ] {{验收项1，如：API 端点正常返回}}
- [ ] {{验收项2，如：数据正确写入数据库}}

### 9.2 性能验证
- [ ] {{性能验收项1，如：响应时间 < 500ms}}
- [ ] {{性能验收项2，如：并发 100 请求成功率 > 99%}}

### 9.3 文档验证
- [ ] {{文档验收项1，如：API 文档已更新}}
- [ ] {{文档验收项2，如：配置示例已添加}}

## 10. 实施计划

### 10.1 时间安排
- **准备阶段**：{{时间，如：0.5 天}}
- **开发阶段**：{{时间，如：2 天}}
- **测试阶段**：{{时间，如：1 天}}
- **部署阶段**：{{时间，如：0.5 天}}

### 10.2 负责人
- {{负责人}}：{{职责}}

## 11. 总结

{{总结内容，说明实施这个功能的价值和预期成果}}
