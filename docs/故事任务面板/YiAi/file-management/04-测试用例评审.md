# 测试用例评审: 文件管理 API

> | v1.0 | 2026-05-13 | deepseek-v4-pro | 🌿 feat/YiAi-file-management |
> 关联: [01-故事任务.md](./01-故事任务.md) · [02-后端技术评审.md](./02-后端技术评审.md)

> **Tester 公式**: Given → When → Then。前置条件 → 操作步骤 → 预期结果。每个用例可独立验证。

---

## 1. 测试范围

### 1.1 功能覆盖矩阵

| FP# | 功能点 | 正常用例 | 边界用例 | 异常用例 | 回归用例 |
|-----|--------|---------|---------|---------|---------|
| FP1 | 图片上传 OSS | TC-N01 | TC-B01, TC-B02 | TC-E01 | — |
| FP2 | 读取文件 | TC-N02, TC-N03 | TC-B03 | TC-E02, TC-E03 | TC-R01 |
| FP3 | 写入文件 | TC-N04 | TC-B04 | TC-E04 | — |
| FP4 | 删除文件 | TC-N05 | TC-B05 | TC-E05 | TC-R02 |
| FP5 | 删除文件夹 | TC-N06 | TC-B06 | TC-E06 | — |
| FP6 | 重命名文件 | TC-N07 | TC-B07 | TC-E07 | — |
| FP7 | 重命名文件夹 | TC-N08 | TC-B08 | TC-E08 | — |
| FP8 | 通用文件上传 | TC-N09 | TC-B09 | TC-E09 | — |

### 1.2 Gate 映射

| Gate | 用例范围 | 通过标准 |
|------|---------|---------|
| Gate A | TC-N01~N09, TC-B01, TC-B03, TC-E01~E03 | P0 用例 100% 通过 |
| Gate B | 全部用例 + 回归用例 | P0 用例 100% 通过，P1 用例 ≥80% 通过 |

---

## 2. 测试用例

### 2.1 正常用例

| ID | Given (前置) | When (操作) | Then (预期) | 关联 FP | 优先级 |
|----|-------------|------------|------------|--------|--------|
| TC-N01 | static/ 目录可写，OSS 已配置 | POST /upload-image-to-oss `{data_url:"data:image/png;base64,iVBORw0KGgo...", filename:"test.png", directory:"test"}` | code=0, data.url 非空且包含 https | FP1 | P0 |
| TC-N02 | static/ 存在 test.txt，内容为 "hello world" | POST /read-file `{target_file:"test.txt"}` | code=0, data.content="hello world", type="text" | FP2 | P0 |
| TC-N03 | static/ 存在 image.png（有效 PNG 文件） | POST /read-file `{target_file:"image.png"}` | code=0, data.content 为静态 URL, type="url" | FP2 | P0 |
| TC-N04 | static/ 目录可写 | POST /write-file `{target_file:"new.txt", content:"test content"}` | code=0, data.message="保存成功" | FP3 | P0 |
| TC-N05 | static/ 存在 to_delete.txt | POST /delete-file `{target_file:"to_delete.txt"}` | code=0, data.message="删除成功" | FP4 | P1 |
| TC-N06 | static/ 存在空目录 to_delete_dir/ | POST /delete-folder `{target_dir:"to_delete_dir"}` | code=0, data.message="删除成功" | FP5 | P1 |
| TC-N07 | static/ 存在 old_name.txt | POST /rename-file `{old_path:"old_name.txt", new_path:"new_name.txt"}` | code=0, data.message="重命名成功", old_path="old_name.txt" | FP6 | P0 |
| TC-N08 | static/ 存在 old_dir/ | POST /rename-folder `{old_dir:"old_dir", new_dir:"new_dir"}` | code=0, data.message="重命名成功" | FP7 | P1 |
| TC-N09 | static/test/ 目录可写 | POST /upload `{filename:"doc.txt", content:"data", target_dir:"test"}` | code=0, data.url="/test/doc.txt" | FP8 | P1 |

### 2.2 边界用例

| ID | Given (前置) | When (操作) | Then (预期) | 关联 FP | 优先级 |
|----|-------------|------------|------------|--------|--------|
| TC-B01 | static/ 可写，OSS 未配置 | POST /upload-image-to-oss 携带有效 Base64 | code=0，回退本地存储，返回 URL 含 `/static/` | FP1 | P0 |
| TC-B02 | static/ 可写 | POST /upload-image-to-oss `{filename:"", directory:""}` | 使用默认值 image.png 和 aicr | FP1 | P1 |
| TC-B03 | static/ 存在含空格路径的文件 | POST /read-file `{target_file:"my file.txt"}` | 空格被替换为 `_`，正确找到文件 | FP2 | P1 |
| TC-B04 | static/ 可写 | POST /write-file `{target_file:"deep/nested/file.txt", content:"x"}` | 自动创建父目录，写入成功 | FP3 | P1 |
| TC-B05 | static/ 存在空文件 | POST /read-file `{target_file:"empty.txt"}` | code=0, content="" | FP2 | P1 |
| TC-B06 | static/ 存在含文件的非空目录 | POST /delete-folder | code=0, 整个目录树被递归删除 | FP5 | P1 |
| TC-B07 | 新旧路径相同 | POST /rename-file `{old_path:"a.txt", new_path:"a.txt"}` | 正常完成或报错（取决于 os.rename 行为） | FP6 | P1 |
| TC-B08 | 新路径父目录不存在 | POST /rename-file `{old_path:"a.txt", new_path:"sub/b.txt"}` | 自动创建 sub/ 目录后重命名成功 | FP6 | P1 |
| TC-B09 | 上传大文件 | POST /upload 含 10MB Base64 内容 | 成功写入（无大小限制） | FP8 | P1 |

### 2.3 异常用例

| ID | Given (前置) | When (操作) | Then (预期) | 关联 FP | 优先级 |
|----|-------------|------------|------------|--------|--------|
| TC-E01 | static/ 可写 | POST /upload-image-to-oss `{data_url:"not-base64!!!"}` | code=1002, message="Base64 解码失败" | FP1 | P0 |
| TC-E02 | static/ 不存在 nonexist.xyz | POST /read-file `{target_file:"nonexist.xyz"}` | code=1002, message 含"文件不存在" | FP2 | P0 |
| TC-E03 | — | POST /read-file `{target_file:"noext"}` | code=1002, message 含"必须包含扩展名" | FP2 | P0 |
| TC-E04 | static/ 不可写（权限 denied） | POST /write-file 写入只读目录 | code=5001, message 含"写入文件失败" | FP3 | P1 |
| TC-E05 | — | POST /delete-file `{target_file:"../etc/hostname"}` | code=1002, message 含"非法路径" | FP4 | P0 |
| TC-E06 | — | POST /delete-folder `{target_dir:"/etc"}` | code=1002, message 含"非法路径" | FP5 | P0 |
| TC-E07 | — | POST /rename-file `{old_path:"../../secret"}` | code=1002, message 含"非法路径" | FP6 | P0 |
| TC-E08 | 源目录不存在 | POST /rename-folder `{old_dir:"nosuch", new_dir:"new"}` | code=1002, message 含"原目录不存在" | FP7 | P1 |
| TC-E09 | — | POST /upload `{target_dir:"../escape"}` | code=1002, message 含"非法路径" | FP8 | P0 |

### 2.4 回归用例

| ID | Given (前置) | When (操作) | Then (预期) | 关联模块 | 优先级 |
|----|-------------|------------|------------|---------|--------|
| TC-R01 | OSS 已配置且有文件 | GEt /?module_name=services.storage.oss_client&method_name=list_files | 文件列表正常返回，不受 upload.py 变更影响 | OSS client | P1 |
| TC-R02 | 已有 sessions 引用 static/ 下图片 | POST /maintenance/cleanup-unused-images `{dry_run:true}` | 清理逻辑正常工作，不受文件删除逻辑变更影响 | maintenance | P1 |

---

## 3. 运行环境专项测试

### 3.1 生命周期

| ID | Given | When | Then | 优先级 |
|----|-------|------|------|--------|
| TC-X01 | 服务启动 | 首次 POST /read-file | 路径安全函数正常工作 | P0 |
| TC-X02 | static/ 目录通过符号链接指向外部 | POST /read-file 请求符号链接路径 | realpath 解析后 commonpath 验证拒绝 | P0 |

### 3.2 存储与数据

| ID | Given | When | Then | 优先级 |
|----|-------|------|------|--------|
| TC-X03 | static/ 目录被删除 | POST /write-file | 自动创建目录，写入成功 | P1 |
| TC-X04 | static/ 磁盘满 | POST /write-file | 返回 code=5001，含错误信息 | P1 |

---

## 4. 测试环境

| 维度 | 配置 |
|------|------|
| 运行环境 | Python 3.10+, FastAPI TestClient |
| 部署方式 | pytest + httpx.AsyncClient (或 TestClient) |
| 测试目标 | http://127.0.0.1:10086 |
| 数据准备 | static/ 目录预置 test.txt, image.png |

---

## 5. 评审清单

| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 每个 FP 至少 1 正常 + 1 边界 + 1 异常用例 | ✅ |
| 2 | Gate A 用例覆盖全部 P0 正常 + 关键边界 | ✅ TC-N01~N02, N04, N07 + TC-B01 + TC-E01~E03 |
| 3 | 回归用例范围与影响链一致 | ✅ OSS client + maintenance |
| 4 | 异常用例包含错误提示和恢复行为 | ✅ |
| 5 | 环境专项：生命周期、存储已覆盖 | ✅ |
| 6 | 无需外部服务即可执行的用例占比 ≥60% | ✅ 仅 TC-N01 需 OSS（有回退） |
