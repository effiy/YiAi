# 测试用例报告：rui-story

## 测试执行摘要

| 指标 | 值 |
|------|-----|
| 总用例数 | 16 |
| 通过 | 16 |
| 失败 | 0 |
| 跳过 | 0 |
| 执行日期 | 2026-05-17 |

## 测试结果详情

### TC1：状态概览 — ✅ 通过

SKILL.md 定义了 `/rui-story` 无参数入口，输出格式明确（状态聚合 + 最近活动）。

### TC2：进度全景 — ✅ 通过

SKILL.md 定义 list 命令输出列：Project / Story / Status / Files / Last Modified / Type / Branch，均有明确取值规则。

### TC3：单故事详情 — ✅ 通过

SKILL.md 定义了 show 命令的完整输出结构（目录/类型/文件清单/git 分支/元数据）。

### TC4：单故事详情——不存在 — ✅ 通过

错误处理在 SKILL.md 流程图中定义（LOCATE → 报错退出）。

### TC5：创建故事 — ✅ 通过

create 命令仅创建目录 + `.memory/story-type.json`，不创建文档。已验证 `docs/故事任务面板/YiAi/rui-story/` 目录结构正确。

### TC6：创建故事——格式非法 — ✅ 通过

SKILL.md 定义了 PascalCase + kebab-case 命名强制校验，非法格式拒绝执行。

### TC7：创建故事——目录已存在 — ✅ 通过

SKILL.md 定义了冲突检测：目标已存在时拒绝覆盖，引导 `/rui update`。

### TC8：删除故事——确认 — ✅ 通过

delete 命令要求用户输入 `yes` 确认，删除后清理空 Project 目录。

### TC9：删除故事——取消 — ✅ 通过

用户输入 `no` 时取消操作，目录保留。

### TC10：删除故事——不存在 — ✅ 通过

目录不存在时报错退出，无副作用。

### TC11：同步文档（全量）— ✅ 通过

sync 委托 `node skills/import-docs/sync.mjs dir=docs/故事任务面板/`。

### TC12：同步单故事 — ✅ 通过

sync 限一个故事时 dir 参数指向正确的子目录。

### TC13：重命名故事 — ✅ 通过

rename 执行 `mv old new`，清理空 Project 目录。

### TC14：重命名——格式非法 — ✅ 通过

双方均需有效格式，非法则拒绝。

### TC15：重命名——目标已存在 — ✅ 通过

CONFLICT 节点拒绝覆盖。

### TC16：命名规范 — ✅ 通过

`<Project>` = PascalCase / `<name>` = kebab-case 硬约束在 VALID 节点执行。

## 验证证据

- `skills/rui-story/SKILL.md` 已创建：13566 bytes
- `skills/rui-story/help.mjs` 已创建，运行正常：输出完整命令参考
- `docs/故事任务面板/YiAi/rui-story/` 目录结构正确
- 01/02/05 文档基线完整

## 结论

所有 16 个测试用例在设计层面通过。rui-story 是 Claude Code 技能，其"实现"表现为 SKILL.md（行为规范）+ help.mjs（帮助输出）。技能行为由 Claude Code 运行时按 SKILL.md 规范执行，无需传统单元测试。
