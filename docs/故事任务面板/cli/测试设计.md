# CLI 工具 — 测试用例评审

## 测试用例

### TC-CL-001：list 命令基本输出

| 项目 | 内容 |
|------|------|
| 目标 | list 命令正常输出表格 |
| 前置 | state_records 集合有数据 |
| 步骤 | `python -m src.cli.state_query list --page-size 5` |
| 预期 | 终端显示 rich 表格，最多 5 行 |

### TC-CL-002：list --format json

| 项目 | 内容 |
|------|------|
| 目标 | JSON 格式输出正确 |
| 步骤 | `python -m src.cli.state_query list --format json --page-size 1` |
| 预期 | 输出合法 JSON 字符串 |

### TC-CL-003：list --format csv

| 项目 | 内容 |
|------|------|
| 目标 | CSV 格式输出正确 |
| 步骤 | `python -m src.cli.state_query list --format csv --page-size 1` |
| 预期 | 输出含 header 的 CSV |

### TC-CL-004：list --output 文件

| 项目 | 内容 |
|------|------|
| 目标 | 结果正确写入文件 |
| 步骤 | `python -m src.cli.state_query list --format json --output /tmp/test.json` |
| 预期 | /tmp/test.json 存在且包含 JSON 数据 |

### TC-CL-005：list 按类型过滤

| 项目 | 内容 |
|------|------|
| 目标 | --record-type 过滤生效 |
| 步骤 | `python -m src.cli.state_query list --record-type test` |
| 预期 | 仅显示 record_type=test 的记录 |

### TC-CL-006：get 命令成功

| 项目 | 内容 |
|------|------|
| 目标 | 按 key 正确获取记录 |
| 前置 | 记录 key="test-key" 存在 |
| 步骤 | `python -m src.cli.state_query get test-key` |
| 预期 | 输出该记录的 JSON |

### TC-CL-007：get 命令记录不存在

| 项目 | 内容 |
|------|------|
| 目标 | 不存在的 key 报错 |
| 步骤 | `python -m src.cli.state_query get nonexistent` |
| 预期 | 红色错误文本，exit code 1 |

### TC-CL-008：export 命令

| 项目 | 内容 |
|------|------|
| 目标 | export 正确导出 |
| 步骤 | `python -m src.cli.state_query export --record-type test --output /tmp/export.json` |
| 预期 | 文件包含所有 test 类型记录 |

### TC-CL-009：stats 命令

| 项目 | 内容 |
|------|------|
| 目标 | stats 正确统计总数 |
| 前置 | state_records 集合有 N 条记录 |
| 步骤 | `python -m src.cli.state_query stats` |
| 预期 | 输出 Total records: N |
