# 后端技术评审 — YiKnowledge 路径迁移

> v1 | 2026-05-09 | 关联: [01-故事任务.md](./01-故事任务.md)

## 变更概述

将 `static.base_dir` 从 `/var/www/YiKnowledge/static` 迁移到 `~/YiKnowledge/static`，并在配置加载时增加 `os.path.expanduser()` 展开。

## 接口清单

无 API 接口变更。仅配置值和加载逻辑变更，对外接口行为不变。

## 数据模型

无数据模型变更。

## 安全约束

| # | 威胁 | 缓解 | 验证方法 |
|---|------|------|---------|
| 1 | `~` 展开后路径遍历 | 现有 `_validate_path` 仍检查 `..` 和绝对路径前缀 | `tests/` 已有路径遍历用例 |

## 影响范围

| 文件 | 变更类型 | 行数 |
|------|---------|------|
| `config.yaml` | 修改 | L31: 路径值 |
| `src/core/config.py` | 修改 | `YamlConfigSettingsSource.__init__`: 新增 expanduser 后处理 |

## 回滚方案

将 `config.yaml` L31 恢复为 `/var/www/YiKnowledge/static`，移除 expanduser 逻辑。

## 评审结论

✅ 通过。T1 微观变更，仅配置值 + 路径展开，无 API 行为变更，安全边界保持。
