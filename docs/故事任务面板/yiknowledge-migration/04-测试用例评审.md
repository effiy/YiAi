# 测试用例评审 — YiKnowledge 路径迁移

> v1 | 2026-05-09 | 关联: [01-故事任务.md](./01-故事任务.md)

## 用例清单

| TC# | 类型 | 描述 | 预期 | 优先级 |
|-----|------|------|------|--------|
| TC1 | 功能 | `settings.static_base_dir` 展开 `~` 为绝对路径 | 值以 `/home/` 开头 | P0 |
| TC2 | 功能 | 服务启动正常挂载 StaticFiles | 无错误日志 | P0 |
| TC3 | 边界 | 路径遍历防护仍有效（`../../../` 被拒） | BusinessException | P1 |
| TC4 | 边界 | `~` 不在路径开头的字符串不受影响 | 其他 config 值正常 | P1 |

## 测试方法

- TC1: 启动 Python 交互环境，检查 `settings.static_base_dir`
- TC2: 启动服务，检查日志无报错
- TC3: 发送路径遍历请求，验证拒绝
- TC4: 验证其他 string 配置项不受 `expanduser` 影响

## 评审结论

✅ 通过。4 条用例覆盖功能、边界场景。
