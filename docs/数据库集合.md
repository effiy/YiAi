# 数据库集合

本文档详细介绍 YiAi 项目的 MongoDB 数据库集合。

---

## 📑 目录

- [用户会话集合](#用户会话集合sessions)
- [RSS 文章集合](#rss-文章集合rss)
- [聊天记录集合](#聊天记录集合chat_records)
- [文件信息集合](#文件信息集合oss_file_info)
- [文件标签集合](#文件标签集合oss_file_tags)
- [宠物数据同步集合](#宠物数据同步集合pet_data_sync)
- [种子数据集合](#种子数据集合seeds)
- [配置说明](#配置说明)

---

## 用户会话集合（sessions）

存储用户会话数据，用于维护用户状态和上下文信息。

### 主要用途
- 保存用户会话状态
- 存储用户上下文数据
- 支持多轮对话的会话连续性

### 配置项
- `collection.sessions` - 集合名称配置

---

## RSS 文章集合（rss）

存储从 RSS 源抓取的文章内容。

### 主要用途
- 保存 RSS 源文章数据
- 存储文章标题、内容、发布时间等元数据
- 支持文章检索和管理

### 配置项
- `collection.rss` - 集合名称配置

---

## 聊天记录集合（chat_records）

存储用户与 AI 的对话历史记录。

### 主要用途
- 保存聊天消息历史
- 记录用户和 AI 的交互内容
- 支持对话回溯和历史查询

### 配置项
- `collection.chat_records` - 集合名称配置

---

## 文件信息集合（oss_file_info）

存储上传文件的元数据信息。

### 主要用途
- 保存文件基本信息（文件名、大小、类型等）
- 记录文件存储路径和访问 URL
- 关联文件标签和描述信息

### 配置项
- `collection.oss_file_info` - 集合名称配置

---

## 文件标签集合（oss_file_tags）

存储文件标签信息，用于文件分类和检索。

### 主要用途
- 管理文件标签分类
- 支持多标签关联
- 便于文件按标签检索

### 配置项
- `collection.oss_file_tags` - 集合名称配置

---

## 宠物数据同步集合（pet_data_sync）

存储宠物相关数据的同步记录（可选功能）。

### 主要用途
- 保存宠物数据同步状态
- 记录同步时间和结果
- 支持数据同步追踪

### 配置项
- `collection.pet_data_sync` - 集合名称配置

---

## 种子数据集合（seeds）

存储系统初始化的种子数据（可选功能）。

### 主要用途
- 保存系统初始数据
- 记录数据种子状态
- 支持数据版本管理

### 配置项
- `collection.seeds` - 集合名称配置

---

## 配置说明

### 配置方式

数据库集合名称通过 `config.yaml` 中的 `collection` 配置项进行配置：

```yaml
collection:
  sessions: "sessions"
  rss: "rss"
  chat_records: "chat_records"
  oss_file_info: "oss_file_info"
  oss_file_tags: "oss_file_tags"
  pet_data_sync: "pet_data_sync"
  seeds: "seeds"
```

### 环境变量覆盖

集合名称也可以通过环境变量覆盖，例如：
- `COLLECTION_SESSIONS` - 覆盖 `collection.sessions`
- `COLLECTION_RSS` - 覆盖 `collection.rss`
- `COLLECTION_CHAT_RECORDS` - 覆盖 `collection.chat_records`

### 相关文件

- `src/core/database.py` - 数据库访问和操作
- `src/core/config.py` - 配置管理
- `config.yaml` - 配置文件示例

---

## 📚 更多信息

- 配置项详细说明请参考 [配置指南.md](./配置指南.md)
