# Nginx 配置示例
# 用于解决 413 Request Entity Too Large 错误
# 
# 使用方法：
# 1. 将此文件复制到你的 nginx 配置目录
# 2. 根据实际情况修改 server_name、proxy_pass 等配置
# 3. 确保 client_max_body_size 设置足够大（建议至少 50M）

server {
    listen 80;
    server_name api.effiy.cn;  # 修改为你的域名

    # 关键配置：增加请求体大小限制
    # 设置为 50M，可根据需要调整（如 100M、200M 等）
    client_max_body_size 50M;
    
    # 增加缓冲区大小，有助于处理大文件上传
    client_body_buffer_size 128k;
    
    # 增加超时时间，防止大文件上传超时
    proxy_connect_timeout 300s;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;
    send_timeout 300s;

    # 代理到 FastAPI 应用
    location / {
        proxy_pass http://127.0.0.1:8000;  # 修改为你的 FastAPI 服务地址
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 支持 WebSocket（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 如果需要 HTTPS，取消下面的注释并配置 SSL
    # listen 443 ssl;
    # ssl_certificate /path/to/your/certificate.crt;
    # ssl_certificate_key /path/to/your/private.key;
}

